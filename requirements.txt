# ── Core ──────────────────────────────────────────────────────────────────────
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0
python-dotenv>=1.0

# ── API ───────────────────────────────────────────────────────────────────────
fastapi>=0.111,<1.0
uvicorn[standard]>=0.30,<1.0
python-multipart>=0.0.9
python-jose[cryptography]>=3.3
passlib[bcrypt]>=1.7

# ── Agent Framework ───────────────────────────────────────────────────────────
langgraph>=0.1,<1.0
langchain>=0.2,<1.0
langchain-anthropic>=0.1
langchain-openai>=0.1
langsmith>=0.1

# ── Cloud SDKs ────────────────────────────────────────────────────────────────
boto3>=1.34
azure-mgmt-costmanagement>=4.0
google-cloud-billing>=1.13

# ── Data & Streaming ──────────────────────────────────────────────────────────
clickhouse-driver>=0.2.7
sqlalchemy>=2.0
alembic>=1.13
kafka-python>=2.0
redis>=5.0
celery>=5.4

# ── ETL ───────────────────────────────────────────────────────────────────────
apache-airflow>=2.9           # orchestration (deploy separately)
# pyspark>=3.5               # heavy dep — install in ETL container only

# ── ML / Forecasting ──────────────────────────────────────────────────────────
prophet>=1.1
xgboost>=2.0
scikit-learn>=1.5
mlflow>=2.13

# ── Observability ─────────────────────────────────────────────────────────────
opentelemetry-sdk>=1.24
opentelemetry-exporter-otlp>=1.24
structlog>=24.0
