apiVersion: v1
kind: Service
metadata:
  name: {{ include "cloudsense.fullname" . }}
  labels:
    {{- include "cloudsense.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cloudsense.selectorLabels" . | nindent 4 }}
